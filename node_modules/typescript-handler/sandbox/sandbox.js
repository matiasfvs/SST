var ts = require('../src/typescript-handler');

var sandbox = ts.compileToDisk({ file: 'sandbox.ts', deleteOnExit: true, compiledPath: 'foo/bleh.js' });
console.log(sandbox.path);

//var result = sandbox.add(1,1);
//console.log(result);
