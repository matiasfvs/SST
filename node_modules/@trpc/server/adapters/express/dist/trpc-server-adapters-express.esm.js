import { n as nodeHTTPRequestHandler } from '../../../dist/nodeHTTPRequestHandler-c752a9ba.esm.js';
import 'url';
import '../../../dist/resolveHTTPResponse-fc7790f6.esm.js';
import '../../../dist/transformTRPCResponse-40817503.esm.js';
import '../../../dist/codes-5678cc97.esm.js';

/* eslint-disable @typescript-eslint/no-explicit-any */
function createExpressMiddleware(opts) {
  return async (req, res) => {
    const endpoint = req.path.slice(1);
    await nodeHTTPRequestHandler({ ...opts,
      req,
      res,
      path: endpoint
    });
  };
}

export { createExpressMiddleware };
